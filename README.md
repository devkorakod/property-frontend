# property-frontend

D1LANDANDHOUSE — part of the property platform monorepo split (see `property-backend`'s sibling docs for system design).

Branches: `main` (production), `staging`, `dev` (default working branch).
